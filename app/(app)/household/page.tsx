import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Household" };

export default function HouseholdPage() {
  return (
    <PagePlaceholder
      title="Household"
      description="Members, roles & permissions, invites, and preferences."
      note="Members and collaboration arrive in Phase 6; preferences in Phases 1–2."
    />
  );
}
