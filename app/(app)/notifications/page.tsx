import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Notifications" };

export default function NotificationsPage() {
  return (
    <PagePlaceholder
      title="Notifications"
      description="Menu changes, member updates, and prep reminders."
      note="In-app notifications arrive in Phase 8."
    />
  );
}
